# Bundled audio tools

This directory is intentionally kept in Git because Tauri validates configured
resource paths during development and CI builds.

Release artifacts are generated by `npm run ffmpeg:runtime` and remain ignored.
They contain the pinned FFmpeg and FFprobe binaries, their license files, and the
runtime manifest.
